# Bard model host manifest

The canonical application catalog lives in the Bard repository. This file is a
human-readable index of the release contents; release assets are addressed by
immutable GitHub release URLs.
